---
title: VideoDelivery
description: Object definition
---

An ECDN server's video delivery status.

### Fields

| NAME | TYPE | NULLABLE | LIST | DESCRIPTION |
| - | - | - | - | - |
| currentNumberOfViewers | [Int](../ecdn-api-scalars/Int) | YES | NO | Current number of users viewing a video through this server (buffering as well). Nullable in case of errors (this metric is coming from an external service). | 
| currentNumberOfBuffering | [Int](../ecdn-api-scalars/Int) | YES | NO | Current number of users experiencing buffering on this server. Nullable in case of errors (this metric is coming from an external service). | 
| numberOfViewers | [ScalarPoint](../ecdn-api-objects/ScalarPoint) | NO | YES | Number of users viewing a video through this server. Nullable in case of errors (this metric is coming from an external service). | 
| numberOfBuffering | [ScalarPoint](../ecdn-api-objects/ScalarPoint) | NO | YES | Number of users experiencing buffering on this server. Nullable in case of errors (this metric is coming from an external service). | 

```graphql
type VideoDelivery {

	# Current number of users viewing a video through this server (buffering as well).
	# Nullable in case of errors (this metric is coming from an external service).
	currentNumberOfViewers: Int

	# Current number of users experiencing buffering on this server.
	# Nullable in case of errors (this metric is coming from an external service).
	currentNumberOfBuffering: Int

	# Number of users viewing a video through this server.
	# Nullable in case of errors (this metric is coming from an external service).
	numberOfViewers: [ScalarPoint!]

	# Number of users experiencing buffering on this server.
	# Nullable in case of errors (this metric is coming from an external service).
	numberOfBuffering: [ScalarPoint!]

}
```

